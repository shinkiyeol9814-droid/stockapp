name: Keep Streamlit Alive

on:
  schedule:
    - cron: '0 1 */5 * *'  # 5일마다 한국시간 오전 10시
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Streamlit App
        run: curl -s -o /dev/null -w "%{http_code}" https://stockapp-sky.streamlit.app/ || true
