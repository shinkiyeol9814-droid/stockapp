[server]
# 💡 keepNodeClean은 더 이상 존재하지 않는 옵션이라 매 실행마다
# "not a valid config option" 경고를 찍고 있었다 — 제거.
# 웹소켓 압축을 해제하여 연결 드랍률을 낮춤
enableWebsocketCompression = false

[browser]
# 불필요한 통계 수집 통신을 꺼서 네트워크 리소스 확보
gatherUsageStats = false

[client]
toolbarMode = "viewer"
